volumes:
  taski_pg_data_production:
  taski_static_volume:

services:
  db:
    image: postgres:13.10
    env_file: .env
    volumes:
      - taski_pg_data_production:/var/lib/postgresql/data
  backend:
    image: lexuniverse/taski_backend
    env_file: .env
    volumes:
      - taski_static_volume:/backend_static
  frontend:
    image: lexuniverse/taski_frontend
    env_file: .env
    command: cp -r /app/build/. /frontend_static/
    volumes:
      - taski_static_volume:/frontend_static/
  gateway:
    image: lexuniverse/taski_gateway
    env_file: .env
    volumes:
      - taski_static_volume:/staticfiles
    ports:
      - 8000:80